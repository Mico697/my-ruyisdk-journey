# 【RuyiSDK 包管理器】 Linux 篇02：安装 RuyiSDK 

---
哈喽，大家好！

上集聊了什么是 RuyiSDK，这集直接动手，把 ruyi 装到电脑上。我整理了三种最常见的安装方式，不管你用的是什么 Linux 发行版，看完都能找到适合自己的方法。

## 方式一：使用预编译的二进制安装（最通用）

以 ISCAS 镜像源为例描述安装流程，首先运行 `uname -m `检查系统架构并下载对应的二进制。

如果输出为：
`x86_64`

```bash
wget https://mirror.iscas.ac.cn/ruyisdk/ruyi/tags/0.51.0/ruyi-0.51.0.amd64
```
下载完成后，请根据上方链接中的实际文件名执行以下命令（命令会随架构自动更新）：

```bash
chmod +x ./ruyi-0.51.0.amd64
```

```bash
sudo cp -v ./ruyi-0.51.0.amd64 /usr/local/bin/ruyi
```
安装 RuyiSDK 包管理器
上方已根据您的架构自动生成命令。确保二进制安装为 ruyi 并位于 PATH 中的目录。

您可以通过以下方式验证安装是否成功：

```bash
ruyi version
```
若命令执行并正常打印了 Copyright 信息则安装已经成功；若出现了非预期的输出则需要检查安装过程和系统环境。


## 方式二：使用Python包管理器安装

安装pipx工具：

```bash
sudo apt install pipx
pipx ensurepath
source ~/.bashrc
```

`pipx`是一个用于安装 Python CLI 工具的工具，它会将每个包安装到独立的环境中，并自动将可执行文件链接到 `PATH`。

```bash
pipx install ruyi
```

原版命令默认走PyPI官方国外源，国内的网络容易超时、下载的比较缓慢;
我们可以替换成国内镜像源，国内下载的速度会得到提升，解决卡顿超时的问题


```bash
pipx install ruyi --pip-args="-i https://pypi.tuna.tsinghua.edu.cn/simple --trusted-host pypi.tuna.tsinghua.edu.cn"
```
安装完成后，ruyi 命令会自动添加到你的 `PATH `中，通常位于 `~/.local/bin`，因此可以通过以下命令验证 `ruyi` 是否安装成功：

```bash
ruyi version
```


## 方式三：系统包管理器安装

从 AUR 安装，以 yay 为例，注意应当使用普通用户：


```bash
yay -S ruyi
```
详细教程可参考：https://ruyisdk.org/docs/Package-Manager/installation#arch-linux


## 常见问题速查

- `pip3: command not found` → `sudo apt install python3-pip`
- `ruyi: command not found` → PATH 没加对，检查 `~/.bashrc`
- 权限不够 → pip 安装时加 `--user`

---

这集就这些，装好了咱们下集见！有任何安装问题欢迎在评论区交流

对应视频： 【RuyiSDK 包管理器】Linux篇02：安装RuyiSDK

---
